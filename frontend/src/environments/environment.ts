// This file defines the base environment configuration for the Angular application.

export const environment = {
  apiURL: "", // Base URL for the API. It is an empty string here and should be set in environment-specific files.
};
