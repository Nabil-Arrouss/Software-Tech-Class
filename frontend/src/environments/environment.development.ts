// This file overrides the base environment configuration for development purposes.

export const environment = {
  apiURL: "http://localhost:8000/api/v1", // Sets the API URL to the local development server running Laravel. The API version (v1) is specified in the URL.
};
